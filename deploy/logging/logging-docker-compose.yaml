version: "3.8"

services:
  elastiksearch:
    networks:
      shared_internal_network:
        - name: home-server-internal-network
        - external: true

  logstash:
    networks:
      shared_internal_network:
        - name: home-server-internal-network
        - external: true

  kibana:
    networks:
      shared_internal_network:
        - name: home-server-internal-network
        - external: true
